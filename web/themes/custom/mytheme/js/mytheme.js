/**
 * @file
 * MyTheme behaviors.
 */
(function (Drupal) {

  'use strict';

  Drupal.behaviors.mytheme = {
    attach (context, settings) {

      console.log('It works!');

    }
  };

} (Drupal));
