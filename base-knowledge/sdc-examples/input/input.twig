<div class="input"{{ attributes }}>
  {% if label %}
    <label class="input__label">{{ label }}</label>
  {% endif %}
  <input type="{{ type }}" placeholder="{{ placeholder }}" class="input__field">
</div>